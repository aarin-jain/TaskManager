{
  "name": "goalcoach",
  "version": "0.1.0",
  "description": "Accountability coach that tracks goals and sends SMS reminders.",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js"
  },
  "dependencies": {
    "better-sqlite3": "latest",
    "cors": "latest",
    "dotenv": "latest",
    "express": "latest",
    "node-cron": "latest",
    "twilio": "latest",
    "zod": "latest"
  },
  "engines": {
    "node": ">=20"
  }
}
